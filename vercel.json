{
    "buildCommand": "next build",
    "outputDirectory": ".next",
    "installCommand": "npm install",
    "devCommand": "next dev",
    "env": [
        "NEXT_PUBLIC_API_URL"
    ],
    "functions": {
        "api/**/*.js": {
            "maxDuration": 60
        }
    }
}